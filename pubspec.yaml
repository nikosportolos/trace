name: trace
description: Trace log messages
version: 0.0.1-prerelease
publish_to: 'none'

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  ansix: ^0.1.0
  date_time_format: ^2.0.1
  meta: ^1.9.1
  path: ^1.8.3
  stack_trace: ^1.11.1

dev_dependencies:
  lints: ^2.1.1
  mocktail: ^1.0.0
  test: ^1.24.6
